<main>
    <article class="home">
        <h1>Lista de produtos</h1>
        
        {{#each  produtos}}
        
        <h2>{{dataValues.nome}}</h2>
        <p>Preço: R${{dataValues.preco}},00</p>
        <hr>
        
        {{/each}}
        
        <div><a href="/produtos"> <button>CADASTRAR PRODUTOS</button></a></div>
    </article>
</main>